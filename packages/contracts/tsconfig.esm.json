{
  "extends": "@banking/config/typescript/library.json",
  "compilerOptions": {
    "outDir": "dist/esm",
    "rootDir": "src"
  },
  "include": ["src"],
  // Tests are compiled by vitest, never shipped in `dist`.
  "exclude": ["src/**/*.spec.ts"]
}
